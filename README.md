# Event Manager
## App is not working as several parts are missing
App was meant to be divided into an admin and client app used to manage and view various events.
It would have a backend API built using Flask and a database to store the details of the events, which could then be accessed by the apps.
In its current state the project is comprised of an admin app to view and manage events, and an API built in Flask for accessing data, however as there is no database, the app does not work.
